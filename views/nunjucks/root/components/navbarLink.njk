{% macro navbarLink(url, href, text) %}
    <li {% if url.includes(href) %}class="active"{% endif %}><a href="{{ href }}" {% if url.includes(href) %}aria-current="page"{% endif %}>{{ text }}</a></li>
{% endmacro %}
